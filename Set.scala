package Collection
import Set._

object Set_Initial { 
  def main(args:Array[String]) {
  var s = Set(11,23,13,14,15); 
  println("Values are:" + s);
  
  
  }
}

//Exception the collection type does not take the values
//solution add the import Array._and change the name of the class